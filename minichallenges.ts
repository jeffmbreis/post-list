//Given a TypeScript class that represents a car, implement a method to calculate its fuel efficiency based on distance traveled and fuel consumed

class Car {
    private fuel: number = 0;

    constructor (
        public brand: string,
        public model: string
    ){}

    fuelEfficien
}